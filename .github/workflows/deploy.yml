name: CI/CD - Docker Build & Push

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
        with:
          install: true
          version: latest

      - name: Cache Docker layers
        uses: actions/cache@v4
        with:
          path: /tmp/.buildx-cache
          key: ${{ runner.os }}-buildx-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-buildx-

      - name: Login to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            ${{ secrets.DOCKER_USERNAME }}/nihongo-app:latest
            ${{ secrets.DOCKER_USERNAME }}/nihongo-app:${{ github.sha }}
          cache-from: type=local,src=/tmp/.buildx-cache
          cache-to: type=local,dest=/tmp/.buildx-cache-new,mode=max

      # Cập nhật cache để chạy nhanh hơn ở lần sau
      - name: Move cache
        run: |
          rm -rf /tmp/.buildx-cache
          mv /tmp/.buildx-cache-new /tmp/.buildx-cache

      # -------------------------------------------------------------
      # NẾU BẠN DEPLOY LÊN SERVER VPS (NHƯ HƯỚNG DẪN TRONG PDF),
      # HÃY BỎ COMMENT PHẦN DƯỚI ĐÂY VÀ CẤU HÌNH SECRETS TƯƠNG ỨNG.
      # -------------------------------------------------------------
      # - name: Deploy to production server
      #   uses: appleboy/ssh-action@v1.0.3
      #   env:
      #     DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
      #     DOCKER_PASSWORD: ${{ secrets.DOCKER_PASSWORD }}
      #   with:
      #     host: ${{ secrets.SERVER_HOST }}
      #     username: ${{ secrets.SERVER_USERNAME }}
      #     key: ${{ secrets.SERVER_SSH_KEY }}
      #     script: |
      #       # Đổi thành thư mục chứa code trên server của bạn
      #       cd /path/to/your/app
      #       
      #       # Đăng nhập Docker Hub
      #       echo "${DOCKER_PASSWORD}" | docker login -u "${DOCKER_USERNAME}" --password-stdin
      #       
      #       # Pull image mới nhất
      #       docker-compose pull
      #       
      #       # Xoá container cũ (nếu có)
      #       docker ps -a --filter "name=nihongo-app" --format '{{.ID}}' | xargs -r docker stop || true
      #       docker ps -a --filter "name=nihongo-app" --format '{{.ID}}' | xargs -r docker rm || true
      #       
      #       # Chạy container mới
      #       docker-compose up -d
      #       
      #       # Xoá image cũ
      #       docker image prune -f
